import { Screen } from 'react-native-screens';
import ThemedText from '@components/common/ThemedText';

export default function ChallengesScreen() {
  return (
    <Screen>
      <ThemedText>Challenges</ThemedText>
    </Screen>
  );
}
